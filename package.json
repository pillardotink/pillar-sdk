{
  "name": "pillar-sdk",
  "version": "1.0.0",
  "description": "Command-line interface SDK for interacting with PillarAI agents",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "qudesdk": "npm run build > /dev/null 2>&1 && node --no-deprecation dist/examples/interact-agent.js"
  },
  "author": "Qudeai",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.0.0",
    "readline-sync": "^1.4.10"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
